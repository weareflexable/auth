apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: auth-web-ingress
  namespace: flexable
  labels:
    app: auth-web
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    alb.ingress.kubernetes.io/group.name: flexable-alb
spec:
  tls:
    - hosts:
        - auth.flexabledats.com
      secretName: auth.flexabledats.com-tls-secret
  rules:
    - host: auth.flexabledats.com
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: auth-web
                port:
                  number: 3000
